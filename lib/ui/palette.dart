import 'package:flutter/material.dart';

class Palette {
  Palette._();

  // Theme colors
  static const primaryButton = Color(0xFF4299E1);
  static const secondaryButton = Color(0xFFED64A6);
  static const inactiveButton = Color(0x80ED64A6);

  // Gradient colors
  static const gradientStart = Color(0x8057B6BF);
  static const gradientEnd = Color(0x80F6C686);

  // Text colors
  static const labelText = Colors.black87;
  static const hintText = Colors.black38;

  // Input colors
  static const inputFill = Color(0xFFF5F5F5);

  // Container colors
  static const background = Colors.white;
  static const shadowColor = Colors.black12;
  static const heroImageBackground = Color(0xFFE0E0E0);
  static const cardOverlay = Colors.black45;
}